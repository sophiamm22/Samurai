@{
  var modelValue = ViewData.TemplateInfo.FormattedModelValue == ViewData.ModelMetadata.Model ?
    String.Format(System.Globalization.CultureInfo.CurrentCulture, "{0:0.00}", ViewData.ModelMetadata.Model) :
    ViewData.TemplateInfo.FormattedModelValue;
}
@Html.TextBox("", modelValue, new { @class = "text-box single-line" })